---
import DarkModeToggle from "./DarkModeToggle.astro";
const SITE_TITLE = "temporary";

interface Props {
  useNavbar?: boolean;
}
const { useNavbar = false } = Astro.props;

const navbar = [
  ["https://wasona.com/", "Course"],
  ["https://lipu.pona.la/", "Library"],
  ["https://sin-kulupu.pona.la/", "News"],
  ["https://discord.gg/6zkugJFU4x", "Discord"],
  ["https://pona.la/", "More"],
];
---

<style>
  header {
    position: relative;
    max-width: 850px;
    text-align: center;
    border-bottom: 5px dotted var(--accent);
    padding: 20px 0;
    margin: auto;
    margin-top: 10px;
  }
  img {
    max-width: 100%;
    max-height: 150px;
  }
  a:has(img) {
    display: block; /*fix focus styling on main header*/
  }
  a {
    text-decoration: none;
    /* color: var(--txt); */
  }
</style>

<header>
  {
    useNavbar && (
      <div style="font-weight: 700">
        {navbar
          .map(([url, label]) =>
            `https://${import.meta.env.PROD}/` == url ? (
              <strong>{label}</strong>
            ) : (
              <a href={url}>{label}</a>
            ),
          )
          .reduce((prev, curr, i) => (
            <>
              {prev} {" · "} {curr}
            </>
          ))}
      </div>
    )
  }
  <a href="/">
    <img alt={SITE_TITLE} src="/header.svg" />
  </a>
  <DarkModeToggle />
  <slot />
</header>
